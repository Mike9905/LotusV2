<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LotusV2.Views.LoginPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Grid VerticalOptions="CenterAndExpand">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Entry Placeholder="Usuario" x:Name="EntryUser" Grid.Row="0"/>
                    <Entry Placeholder="Contraseña" x:Name="EntryPassword" IsPassword="True" Grid.Row="1"/>
                    <Button Text="Iniciar Sesion" Grid.Row="2" BackgroundColor="Green" TextColor="White" Clicked="Button_Clicked_1"/>
                    <Button Text="Registrarse" Grid.Row="3" Clicked="Button_Clicked"/>
                </Grid>
            </StackLayout>
        </ScrollView>
        
    </ContentPage.Content>
</ContentPage>